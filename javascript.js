// Variáveis:
var nome = "Matheus Alves";
var idade = 20;

// Janela de alerta com concatenação entre string e variáveis utilizando o "+"
alert(nome + " tem " + idade + " anos");

// Janela de prompt (para o usuário escrever uma mensagem)
prompt("Quantos anos você tem? ");

// Arrays:

